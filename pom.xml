<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings - H2 in memory -->
        <property name="hibernate.connection.driver_class">org.h2.Driver</property>
        <property name="hibernate.connection.url">jdbc:h2:mem:e=SistemadeReservasdeRestaurante;DB_CLOSE_DELAY=-1</property>
        <property name="hibernate.connection.postgres"></property>
        <property name="hibernate.connection.260105"></property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.H2Dialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Update the database schema on startup -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Current session context configuration -->
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- Connection pool settings -->
        <property name="hibernate.connection.pool_size">10</property>
        <property name="hibernate.connection.autocommit">false</property>

        <!-- Entity mappings -->
        <mapping class="model.Cliente"/>
        <mapping class="model.Mesa"/>
        <mapping class="model.Reserva"/>
        <mapping class="model.FormaPagamento"/>
        <mapping class="model.Funcionario"/>
        <mapping class="model.Restaurante"/>
    </session-factory>
</hibernate-configuration>